let state = 0;

function setup() {
  createCanvas(500, 500);
  textAlign(CENTER);
  textSize(30);
}

function draw() {
switch(state){
  case 0:

  break;
background ('salmon');
text ("why did the chicken cross the road?", width/2, height/2);
   case 1:
   background ('salmon');
   text ("why did the chicken cross the road?", width/2, height/2);
   break;
}
}
